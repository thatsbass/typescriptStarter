# Initialize a TypeScript configuration file (tsconfig.json) with default settings
tsc --init
# Initialize a new Node.js project with default settings (creates a package.json file)
npm init -y
# Run the TypeScript build script defined in package.json (assumes a build script is set up)
npm run build:ts

